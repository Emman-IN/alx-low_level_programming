#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @int: parameter to be evaluated
 * Return: 0 Success always
 */

int _abs(int)
{
	n;

	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
