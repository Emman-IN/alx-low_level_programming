#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguement count
 * @argv: arguement vector (array of arguement strings)
 * Return: 0 success
 */

int main(int argc, char *argv[])
{
	printf("%s", argv[0]);
	return (0);
}
