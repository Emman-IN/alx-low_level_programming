#include "main.h"

/**
 * _isdigit - checks for numbers between 0 - 9
 * @c: number to be evaluated
 * Return: 1 if digit 0 otherwise
 */

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (1);
	}
	_putchar('\n');
}
