#include<stdio.h>

/**
* main - Entry point
*
* Return: Always 0 (Success)
*/

int main(void)
{
	int n, m;

	for (n = 97, m = 65; n <= 122, m = 90; m++, n++)
		putchar(n)
		putchar(m)
	putchar('\n')
	return (0)
}
